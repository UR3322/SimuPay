{
  "name": "simupay",
  "version": "1.0.0",
  "description": "SimuPay - Payment Simulation Application",
  "scripts": {
    "client": "cd frontend && npm start",
    "server": "cd backend && npm start",
    "install-all": "cd backend && npm install && cd ../frontend && npm install",
    "dev": "concurrently \"npm run server\" \"npm run client\""
  },
  "keywords": ["mern", "payment", "simulation", "react", "nodejs"],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
